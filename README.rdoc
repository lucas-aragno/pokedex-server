== How to set it up

* Install the gems:

```
bundle install
```

* Run the migrations:

```
bundle exec rake db:migrate
```

* Start the server:

```
rails s
```

**You can also check out the routes by running:**

```
bundle exec rake routes
```
